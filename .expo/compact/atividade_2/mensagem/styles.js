import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    mensagem: {
        fontSize: 25,
        margin: 10,
        color: '#59b3b0',
    },
    viewmensagem: {
        borderWidth: 3,
        borderColor: '#48d138',
        borderRadius: 30,
        marginBottom: 10,
    },
});

export default styles;