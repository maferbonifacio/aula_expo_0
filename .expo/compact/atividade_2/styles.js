import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    titulo: {
        fontSize: 30,
    }
    mensagem: {
        fontSize: 25,
        margin: 10,
        color: 'blue',
    },
    viewmensagem: {
        borderWidth: 2,
        borderColor: '#ff2bc0',
        borderRadius: 20,
    },
});

export default styles;