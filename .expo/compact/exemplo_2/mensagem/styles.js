import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    mensagem: {
        fontSize: 25,
        margin: 10,
        color: 'blue',
    },
    viewmensagem: {
        borderWidth: 2,
        borderColor: '#ff2bc0',
        borderRadius: 20,
        marginBottom: 10,
    },
});

export default styles;